rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Uploads and private proof downloads go through authenticated server routes.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
